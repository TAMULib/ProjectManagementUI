language: node_js

node_js:
  - "10"

addons:
  chrome: stable

services:
  - xvfb

before_script:
  - npm start > /dev/null &
  - npm run update-webdriver
  - sleep 1 # give server time to start

script:
  - npm run test-ci
  - grunt coverage
